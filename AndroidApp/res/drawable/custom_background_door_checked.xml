<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background shape -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="20dp" /> <!-- Adjust the radius as needed -->
            <solid android:color="@color/white" /> <!-- Set your desired button color -->
            <size android:width="100dp" android:height="100dp" /> <!-- Adjust the size as needed -->
        </shape>
    </item>
    <!-- Image -->
    <item android:drawable="@drawable/door_open"
        android:gravity="center"
        android:width="50dp"
        android:height="80dp" />
</layer-list>
